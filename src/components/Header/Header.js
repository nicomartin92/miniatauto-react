import React from 'react';
import './Header.scss';

const Headers = () => {
    return (
        <header className="header"
            style={{ color: '#000000' }}>
            header
        </header>
    )
}

export default Headers;